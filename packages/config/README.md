# Config Package

Shared configuration utilities and constants.

## Getting Started
- Run tests: `npx nx test config`
- Build artifacts: `npx nx build config`

## Next up
- Centralize environment variable schemas and defaults.
- Provide helpers for loading config in Node services and Angular apps.
- Add sanity checks for required secrets before startup.
