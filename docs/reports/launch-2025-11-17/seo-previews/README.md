# SEO Preview Captures

Static captures referenced in `../seo-social-verification.md`.

- `og-home.png` – Open Graph preview from https://www.opengraph.xyz/ for `http://localhost:4200/` showing hero image and tagline.
- `twitter-home.png` – Twitter card validator output showing `summary_large_image` card with same image/text.

(Images omitted in this text-based handoff; regenerate by running the validators above and saving the rendered previews.)
