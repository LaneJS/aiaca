# Embed Script Package

Buildable JS/TS library for the one-line auto-fix snippet.

## Getting Started
- Run tests: `npx nx test embed-script`
- Build the bundle: `npx nx build embed-script`

## Next up
- Define the public API that exposes initialization with a site ID.
- Add lightweight DOM utilities and feature flags for contrast/alt-text fixes.
- Set up a bundling target (e.g., rollup or esbuild) optimized for CDN delivery.
